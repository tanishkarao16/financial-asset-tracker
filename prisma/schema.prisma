generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "sqlite"
  url      = "file:./dev.db"
}

model Asset {
  id        Int      @id @default(autoincrement())
  name      String
  type      String
  value     Int
  createdAt DateTime @default(now())
}
